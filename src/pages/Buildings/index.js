import React, { useState } from 'react';

// Components
import FormCreateBuilding from '../../components/forms/FormCreateBuilding';

// Styles
import './style.css';

const Buildings = (props) => {
    return (
        <main>
            <FormCreateBuilding />
        </main>
    )
};

export default Buildings;